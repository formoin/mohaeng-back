<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.chat.model.mapper.ChatMapper">


    <select id="getChatting" resultType="chat">
        select chat_id, content, write_time, group_id, user_id
        from chat
        where group_id=#{groupId}
    </select>

    <delete id="deleteChat" parameterType="int">
        delete from chat
        where chat_id=#{chatId}
    </delete>

    <insert id="createChat" parameterType="chat">
        insert into chat (content, group_id, user_id)
        values (#{content}, #{groupId}, #{userId})
    </insert>

</mapper>